# Estado do Projeto — Plataforma de Inglês

> **Para uma sessão nova do Claude Code**: leia este ficheiro primeiro, depois `docs/decisions.md` (histórico de decisões técnicas) e `docs/10-scope-mvp1.md` (o que está dentro/fora do MVP1). Este ficheiro deve ser atualizado sempre que houver uma mudança relevante na app — não deixar desatualizado.

Última atualização: 2026-08-27, **Fase 14 continuada: mais 3 textos de leitura.** `ai-in-the-workplace-news` (B2, "news"), `borrowing-money-dialogue` (B1, "dialogue"), `changing-doctor-email` (A2, "email") — todos originais. Total: 76→79. Antes, na mesma sessão: **bug real corrigido: Diagnóstico Semanal e Sheets de tema misturavam qualquer nível CEFR.** `buildQuestionSet` nunca filtrava exercícios por nível — com B2 introduzido nesta sessão (inversão, cleft sentences, participle clauses), um utilizador Pre-A1 podia genuinamente receber uma pergunta B2 no Diagnóstico Semanal. Corrigido: novo parâmetro `userLevel` filtra para o nível do utilizador e abaixo (nunca acima), com recuo seguro se não houver conteúdo suficiente nesse nível. 4 testes unitários novos. Antes, na mesma sessão: **Fase 14 continuada: mais 3 textos de leitura.** `power-cut-instructions` (A2, "instructions"), `neighbour-noise-dialogue` (B1, "dialogue"), `conference-registration-email` (B2, "email") — todos originais. Total: 73→76. Antes, na mesma sessão: **bug real corrigido: o nível do utilizador nunca avançava depois do placement test.** `currentLevel`/`currentSublevel` (`LearningProfile`) só eram escritos uma vez, na submissão do teste de nivelamento — nunca mais mudavam, mesmo que o utilizador completasse currículo inteiro de níveis seguintes. Mesmo padrão "conta mas não age" já corrigido para `UserError.occurrences` na Fase 11. Corrigido em `maybeIssueCertificate` (`lib/certificate.ts`): ganhar o certificado do nível atual agora avança para o subnível seguinte (via `Sublevel.order`, sequencial e único), até ao teto atual do currículo (B2.2). Antes, na mesma sessão: **Fase 13 continuada: mais 3 módulos B2 (Future Perfect, Discourse Markers, The More The More).** B2 aprofundado: B2.1 3→4 módulos, B2.2 3→5 módulos. Future Perfect ("will have done") só aparecia como distrator errado noutros exercícios, nunca teve módulo próprio. Discourse Markers (however/furthermore/as a result) — essencial para escrita formal. "The more..., the more..." — correlativos comparativos, gap real. Currículo: 48→51 lições. Verificação exaustiva: 665 ids em 51 módulos, zero duplicatas. Antes, na mesma sessão: **Fase 14 continuada: mais 3 textos, primeiros ao nível B2.** `remote-work-debate-news` (B2, "news"), `salary-negotiation-dialogue` (B2, "dialogue"), `returning-a-parcel-email` (A2, "email") — os dois primeiros são os primeiros textos de leitura de nível B2, agora que esse nível existe no currículo. Total de textos de leitura: 70→73. Antes, na mesma sessão: **placement test ganha perguntas de dificuldade B2 (fecha lacuna documentada no commit anterior).** 5 perguntas novas (uma por pilar: grammar/vocabulary/listening/reading/translation), testando pontos só ensinados nos módulos B2 desta sessão. `DIFFICULTY_WEIGHT` ganha B2:4. Novo teste confirma que estas perguntas discriminam de verdade B1 forte de B2 a sério (sem elas, o teste não distinguia os dois). Placement test: 18→23 perguntas, currículo/placement agora totalmente consistentes (Pre-A1 a B2 nos dois lados). Antes, na mesma sessão: **fonologia de fala ligada (connected speech) na referência de pronúncia.** O veredito honesto da auditoria dizia que o que falta para perceber filmes/música é, por ordem, áudio real, currículo até B2 com fala ligada, e material autêntico — os 8 itens existentes em `/practice/pronunciation` eram todos sobre sons isolados, nada sobre como as palavras se ligam/reduzem na fala corrida (linking, weak forms, elision, contrações faladas gonna/wanna, sons de ligação intrusivos). 5 itens novos adicionados, com secção própria na página. Antes, na mesma sessão: **marco maior: nível B2 introduzido (6 módulos) + bug real corrigido no placement test.** O currículo tinha B1 como teto desde a Fase 4/9, apesar de o schema já suportar `CefrLevel.B2` desde a Fase 0. Introduzido agora com 2 sublevels (B2.1: Mixed Conditionals, Inversion for Emphasis, Should Have/Needn't Have; B2.2: Cleft Sentences, Participle Clauses, Advanced Reporting Verbs) — 6 pontos gramaticais B2 genuínos, todos distintos do que já existia em B1. **Bug real encontrado e corrigido**: `src/lib/placement/scoring.ts` tinha o teste de nivelamento preso em A2.2 como teto absoluto — mesmo um utilizador que acertasse TODAS as perguntas (incluindo as 5 de dificuldade B1) nunca era colocado acima de A2.2, apesar de B1 já existir no currículo desde a Fase 4/9. Corrigido, com teste unitário novo a confirmar. Currículo: 42→48 lições (Pre-A1 a B2). Verificação exaustiva: 626 ids em 48 módulos, zero duplicatas, zero referências partidas. Antes, na mesma sessão: **balanço da ronda anterior: 6 módulos de gramática + 3 textos de leitura novos, verificação final de integridade.** Módulos novos: So Do I / Neither Do I (B1.1), Indirect Questions (B1.2), Adjective Order (A2.2), Reflexive Pronouns (A1.1) — todos gaps reais de gramática confirmados por revisão sistemática, cada um ligado a um erro PT→EN documentado. Currículo por nível: Pre-A1 2, A1 15, A2 12, B1 13 — **total 42 lições** (era 30 no início da Fase 13), rebalanceando exatamente o que a auditoria pedia (Pre-A1 1→2, A2 7→12, quase ao nível de A1). Textos de leitura: 70 (era 60). Verificação final exaustiva: 548 ids em 42 módulos sem duplicatas, zero `concept_ref`/`vocabulary_ids`/`exercise_ids` partidos, 70 textos de leitura sem ids duplicados. Antes, na mesma sessão: **Fase 14 continuada: mais 3 textos de leitura autênticos.** `flat-share-dialogue` (A2, "dialogue"), `library-instructions` (A1, "instructions"), `office-relocation-email` (B1, "email") — todos originais. Total de textos de leitura: 67→70. Antes, na mesma sessão: **Fase 13 continuada: módulo Reflexive Pronouns (A1.1).** O módulo de pronomes existente cobria objeto/possessivos, mas nunca reflexivos (myself, yourself, himself...) — o português marca reflexividade no próprio verbo, não com pronome à parte, o que causa erros nos dois sentidos (esquecer o reflexivo quando é preciso, ou usá-lo onde não é preciso). Currículo: 41→42 lições. Antes, na mesma sessão: **Fase 13 continuada: módulos Indirect Questions (B1.2) e Adjective Order (A2.2).** Perguntas indiretas educadas ("Could you tell me where the station is?", sem inversão nem do/does/did) — gap de alto valor prático (viagens, atendimento) — e ordem de adjetivos (opinião→tamanho→idade→forma→cor→origem→material, ex. "a beautiful small old round French wooden table") — o português tem ordem livre pós-substantivo, o inglês tem ordem fixa pré-substantivo. Currículo: 39→41 lições. Verificação exaustiva repetida: 535 ids em 41 módulos, zero duplicatas, zero referências partidas. Antes, na mesma sessão: **Fase 13 continuada: módulo So Do I / Neither Do I (B1.1).** Gap real: a estrutura de concordância com auxiliar ("So do I", "Neither have I") nunca teve módulo próprio, apesar de muito comum na conversa e de exigir inversão (auxiliar antes do sujeito) — o português não distingue estruturalmente "eu também"/"eu também não" consoante o tempo verbal. Currículo: 38→39 lições. Reverificado por completo: 509 ids em 39 módulos, sem duplicatas. Antes, na mesma sessão: **verificação de integridade de todo o currículo, fim da ronda de gramática da Fase 13.** Depois de 8 módulos novos nesta sessão (30→38 lições), corrida uma verificação exaustiva sobre os 38 ficheiros de `content/curriculum/`: 496 ids (module/unit/grammar_concept/vocabulary/exercise/lesson) sem nenhuma duplicata; os 114 `concept_ref` das exercises resolvem todos para um `grammar_concept.id` real; todos os `vocabulary_ids`/`exercise_ids` referenciados nos `lesson.steps` existem. `readingPassages.ts`: 67 textos, ids sem duplicatas. **Com isto fecha-se esta ronda**: todos os achados concretos e citados textualmente na auditoria de 2026-08-27 estão fechados (Fases 8-12 completas, achado N6, rubrica com 5 eixos, ação #4 do plano diário), e as Fases 13/14 (trabalho de escala, sem prazo de uma sessão) têm cada uma vários lotes sólidos e verificados. Próxima sessão: continuar Fase 13 (mais lições, currículo B2) ou Fase 14 (mais conteúdo autêntico) é trabalho de volume, não de correção de bugs — usar o mesmo processo (schema idêntico ao de um módulo já existente, `ConvertFrom-Json` para validar, grep para confirmar ids únicos antes do commit). Antes, na mesma sessão: **Fase 13 continuada: módulo Have/Get Something Done — causativo (B1.2).** A estrutura "have/get + objeto + particípio passado" (dizer que outra pessoa fez algo por si — "I had my hair cut") nunca teve módulo próprio, apesar de extremamente comum no dia a dia; o português não distingue gramaticalmente entre fazer algo pessoalmente ou mandar fazer, o que causa confusão real. Currículo: 37→38 lições. Antes, na mesma sessão: **Fase 13 continuada: módulo Wish Clauses (B1.2).** "Wish" (desejos sobre o presente, arrependimento sobre o passado) nunca teve módulo próprio, apesar de ser um ponto B1 clássico e intimamente ligado ao Second/Third Conditional já seedados (mesma lógica de "recuar um tempo verbal"). Currículo: 36→37 lições. Nota de processo: o rascunho inicial reutilizava por engano um id (`vocab_b1_regret`) já existente no módulo third-conditional — apanhado pelo grep de verificação já usado em todos os lotes de conteúdo desta sessão, antes de qualquer commit. Antes, na mesma sessão: **Fase 13 continuada: módulo Present Perfect Continuous (B1.1).** Gap real: só existia Present Perfect Simples (A2) e a distinção Present Perfect vs. Past Simple (A2, também adicionada nesta sessão), nunca a forma contínua (`have/has been + -ing`) — tempo verbal B1 típico, sem equivalente direto em português. Currículo: 35→36 lições. Antes, na mesma sessão: **Fase 14 continuada: mais 4 textos de leitura autênticos.** `recipe-instructions` (A1, género "instructions", usado pela 1ª vez), `job-interview-dialogue` (B1, "dialogue"), `gym-membership-email` (A2, "email"), `weather-warning-news` (B1, "news") — todos originais. Total de textos de leitura: 63→67. Antes, na mesma sessão: **ação #4 da auditoria corrigida: plano diário liga a sério ao pilar mais fraco.** O item "Tema à escolha (pilar mais fraco)" na Home prometia adaptação mas ligava sempre para o seletor genérico `/practice/topic` — nunca passava `weakAreas` a `generateDailyPlan` nem usava `/practice/topic/[pillar]`, que já existia desde a Fase 8. Corrigido: `generateDailyPlan` recebe `weakAreas` (retrocompatível, default `[]`), usa o primeiro pilar fraco elegível (só os 5 com exercícios discretos — GRAMMAR/VOCABULARY/LISTENING/READING/TRANSLATION), 2 testes novos. Era o único item ainda por fazer da lista "Top 5 ações imediatas" da auditoria. Antes, na mesma sessão: **rubrica ganha 5º eixo: Naturalidade.** A auditoria notava que o prompt à IA sempre pediu para avaliar naturalidade/idiomaticidade, mas nenhum número correspondente existia na rubrica (só gramática/vocabulário/coerência/cumpre-o-pedido). `NATURALNESS` adicionado como 5ª dimensão, na mesma posição lógica das outras — `WritingStep`/`SpeakingStep` ganham a 5ª barra automaticamente (já iteram `Object.keys(RUBRIC_LABEL)`). **Com isto, todos os achados concretos e citados textualmente na auditoria de 2026-08-27 (Fases 8-12 completas, achado N6, rubrica) estão fechados** — o que resta é trabalho de escala (Fases 13/14 completas: 80-120 lições, áudio real, conteúdo autêntico), já com um primeiro lote sólido feito em cada uma. Antes, na mesma sessão: **achado N6 corrigido: seed paralelizado.** A auditoria apontava o seed (corre a cada deploy Netlify) como ~2.400 upserts 100% sequenciais, risco real de build lento/timeout que só cresce com o currículo (agora 35 módulos). `seedVocabularyBank` (2000+ palavras) passa a correr em lotes de 25 em paralelo; os 35 módulos (independentes entre si) em lotes de 4. **[POR CONFIRMAR EM RUNTIME]** — sem Node local nem deploy Netlify disponível (pausado até 2026-09-01) para medir o tempo de build real antes/depois; a mudança foi revista com cuidado por leitura, sem alterar a lógica de upsert, só a forma como as chamadas são disparadas. Antes, na mesma sessão: **FASE 13 continuada: mais 2 módulos A2 (Zero Conditional, Present Perfect vs. Past Simple).** Currículo: 33→35 lições. Dois gaps reais de gramática encontrados ao rever a cobertura: só existiam 1st/2nd/3rd conditional (faltava o Zero, tipicamente ensinado primeiro por ser mais simples); e Present Perfect/Past Simple eram ensinados isoladamente mas nunca a distinção entre os dois — um dos pontos mais confusos para falantes de português, que usam o mesmo tempo verbal (Pretérito Perfeito Simples) para ambos os casos. Antes, na mesma sessão: **FASE 14 (Inglês autêntico) — iniciada, 1º lote.** Campo `genre`/`source` em `ReadingPassage` (pedido explícito da auditoria), opcional e não retroativo — os 60 textos existentes continuam válidos sem ele. Usado pela 1ª vez em 3 textos originais novos, em formatos que nunca existiam no currículo (só havia narrativa em 3ª pessoa): um email de trabalho, um diálogo a planear uma viagem, uma notícia local — nenhum copiado de fonte real (nunca reproduzir letras de música ou notícias reais por direitos de autor). `ReadingRunner.tsx` ganhou `whitespace-pre-line` para preservar as quebras de linha do diálogo/email. **Letras de música, notícias/podcasts graduados de verdade e legendas em 3 camadas ficam fora de alcance sem áudio real** (Fase 9, bloqueada por decisão financeira do utilizador — não pode ser feito a custo zero). Antes, na mesma sessão: **FASE 13 (Currículo até B2) — iniciada, 1º lote.** A auditoria pedia 30→80-120 lições (~2-3 meses, P2) — trabalho grande demais para uma sessão, por isso começou-se pelos gaps mais concretos e citados explicitamente: Pre-A1 tinha só 1 módulo (agora 2 — números/dias/horas, gramática "there is/there are"), e faltavam por completo um módulo de **phrasal verbs** (A2.2 — turn off, look after, give up, look into, get back to) e um de **modais de capacidade** (A2.1 — can/could/be able to, incluindo o erro clássico "I will can"). Currículo: 30→33 lições. Os 3 ficheiros JSON novos foram validados com PowerShell `ConvertFrom-Json` (sem Node local) e os ids confirmados únicos por grep antes do commit — mesmo processo já usado nas fases de conteúdo anteriores desta sessão. **Falta ainda, fora do escopo deste lote**: rebalancear A2 até aproximar de A1 (14), fonologia de fala ligada (elisão, reduções), currículo B2 completo. Antes, na mesma sessão: **FASE 12 (Retenção) CONCLUÍDA.** Streak ganha "congelamentos" (1 por semana seguida, máx. 2 guardados, gasto automaticamente se falhar exatamente 1 dia — `LearningProfile.streakFreezes`, novo campo, mostrado com ❄️ em `StreakXp`). `/progress` volta a mostrar o "porquê" do onboarding (`goal`/`targetDate`), capturado desde sempre mas nunca devolvido ao utilizador fora do prompt oculto do tutor. `/profiles` ganha um "Voltar" seguro (antes, uma conta com 1 perfil que lá chegasse via "Gerir perfis" ficava sem saída). Perfil passa a poder ser renomeado em Definições. Antes, na mesma sessão: **FASE 11 (Accountability de erros) CONCLUÍDA.** `UserError.occurrences` existia desde sempre mas só era lido para imprimir "3x" no ecrã — agora **age**: `getDueReviews` prioriza a fila por `occurrences` e força a repescagem de erros com 3+ ocorrências ainda não resolvidos mesmo antes do SM-2 os marcar como due; `buildQuestionSet` (Diagnóstico Semanal + Sheets de tema — as duas superfícies de "testa-me" da app) passa a consultar os erros persistentes do utilizador e a dar-lhes prioridade nas perguntas escolhidas; `errorType` para pilares não-gramaticais deixa de colapsar sempre na string literal do pilar ("vocabulary"/"listening"/...) — usa a tag mais específica já existente no conteúdo (ex. "hobbies", "work"). Antes, na mesma sessão: **FASE 10 (Rubrica e oral a sério) CONCLUÍDA.** Rubrica de 4 eixos (Gramática/Vocabulário/Coerência/Cumpre o pedido), que só existia em writing, estendida a speaking — `getHolisticFeedback` pede e faz parse dos mesmos 4 eixos para speaking, com ordem exata das linhas finais definida no prompt (rubrica → PRONUNCIATION → SCORE, sem ambiguidade) para o modelo não confundir os dois conjuntos de marcadores agora usados ao mesmo tempo em speaking. `SpeakingStep` (`LessonRunner.tsx`) passa a mostrar as 4 barras, tal como `WritingStep`. **AI Tutor ganhou canal de voz**: `RecordButton` junto ao campo de texto preenche a pergunta por voz (sem enviar sozinho, dá hipótese de rever), e cada mensagem do tutor ganha um botão "🔊 Ouvir" que lê o texto respeitando o sotaque do onboarding. Fecha o achado E da auditoria de 2026-08-27 por completo. Antes, na mesma sessão: **FASE 9 (Áudio e Speaking) — parcial, dentro do custo zero.** 3 achados concretos da auditoria resolvidos sem sair do orçamento: TTS passa a respeitar o sotaque escolhido no onboarding (`EnglishVariantContext`, novo), shadowing deixa de dar sempre 65 (agora compara o transcript com a frase alvo), `pronunciationScore`/`fluencyScore` deixam de ficar escondidos até `/progress` — aparecem já junto do feedback de cada resposta falada. **O que fica por fazer, bloqueado por decisão do utilizador, não por esquecimento**: áudio pré-produzido / voz humana real exige contratar um serviço de TTS pago (Azure/ElevenLabs) — decisão financeira que não é tomada unilateralmente. Detalhe em `docs/decisions.md`. Antes, na mesma sessão: **FASE 8 (Blindagem) CONCLUÍDA — regressão de segurança crítica corrigida.**

> ✅ **Resolvido**: a vulnerabilidade central da 1ª auditoria (certificados forjáveis do browser), reaberta no commit `3753fb3` desta sessão via `completeMicroChallenge(pillar, score)` e 4 rotas semelhantes (`dictation`, `reading`, `daily-challenge`, `idioms`), está corrigida — pilar e nota passam a ser sempre derivados no servidor a partir do conteúdo real, nunca aceites do cliente. `verbs/actions.ts` ficou deliberadamente inalterado (autoavaliação booleana, mesmo padrão já aceite para a confiança em speaking). Também corrigido: pilar não verificado no Diagnóstico Semanal/prática por tema, injeção via `prompt` em submitWriting/submitSpeaking, `sessionFocus`/`ERROR_LOGGED` sem validação no tutor, onboarding sem validação nenhuma, open redirect com `\`, rate limit não atómico, IDOR na fila SRS, 10 componentes sem tratamento de erro de rede. **Primeiros testes automáticos do projeto** (vitest, `npm test`) — a escrever o teste do plano diário, foi encontrado e corrigido um bug real (minutos negativos para 16-19min com revisões pendentes). Revisão independente por 2 agentes antes do commit, dado o volume (34 ficheiros) e a criticidade. Detalhe completo em `docs/decisions.md` ("Fase 8").

Resumo da auditoria de 2026-08-27 (nota global 4,2 → 5,6/10, relatório em `docs/AUDITORIA-2026-08-27.md`): ganhos reais em conteúdo (11→30 lições, 331→2.013 palavras, 4→60 textos, 8/24→22/24 conceitos gramaticais) e em UX/reading/writing/gramática. **Dois itens prometidos e NÃO feitos**: zero testes automáticos e `zod` instalado mas nunca importado (ambos da Fase 2). **Item nuclear da Fase 4 não feito**: zero áudio real — 100% TTS do browser, uma só voz `en-US`, `audio_url: null` nos 30 módulos. **Contraste**: 5 das 6 combinações cor-de-acento/fundo falham WCAG AA (a Fase 7, feita por mim, subnotificou isto — só reportei 1). Próximas fases 8-14 definidas no relatório. Antes desta auditoria: **FASE 7 CONCLUÍDA: polimento (acessibilidade, performance, código morto)** — última fase do roadmap da auditoria original. Corrigido: `aria-current="page"` no `BottomNav`, `<label>` em falta no formulário de `/profiles`, 1 export morto removido (`MicroChallengeKind`). Confirmado já estar bem (sem alterações): foco visível global consistente (`globals.css`), `lang="pt-PT"`, sem imagens sem `alt` (a app não usa raster), `prefers-reduced-motion`, zero N+1 de queries, zero referências residuais aos 5 modelos Prisma removidos antes. **1 achado de acessibilidade sinalizado mas não corrigido, por ser decisão de marca**: texto `verdigris` sobre `linen` tem contraste ~4.37:1 (abaixo do mínimo AA de 4.5:1 para texto normal) — a cor de marca principal, usada em centenas de sítios; corrigir exigiria escurecer ligeiramente o tom, uma decisão visual que só o utilizador deve tomar (mesmo critério já usado para outras mudanças de identidade visual nesta sessão). Número exato e correção sugerida em `docs/decisions.md`. **Com isto, as Fases 1-7 do roadmap da auditoria de 2026-08-26 estão todas concluídas.** Antes, na mesma sessão: **FASE 6 CONCLUÍDA: perfis múltiplos por conta ("Família", estilo Netflix)** — a maior mudança de arquitetura da sessão, pedida explicitamente pelo utilizador (consultado antes de avançar, dado o risco maior desta mudança face a tudo o resto: schema + ~15 tabelas de progresso + ~47 ficheiros consumidores). Uma conta (login Supabase) pode agora ter vários `Profile` — cada membro da família com o seu próprio currículo, XP, streak, erros, certificados, conversas com o tutor, totalmente isolados. Contas com 1 perfil (o caso de sempre) não veem nenhuma mudança visível — o perfil único é criado/escolhido automaticamente. Com 2+, `/profiles` mostra o seletor. "Trocar de perfil" em Definições. Eliminar dados (RGPD) passa a apagar só o perfil ativo, não a conta inteira. Estratégia de migração deliberadamente de baixo risco (`userId` manteve o nome em todas as tabelas, só o alvo da FK mudou de User para Profile — evitou tocar em dezenas de ficheiros), revista por um agente independente antes do commit dado não haver build local nem deploy real para testar. Detalhe completo em `docs/decisions.md`. Antes, na mesma sessão: **teto global de IA + 3 módulos B1 novos**. Fechado o último item ALTO ainda por corrigir da auditoria original (#6: sem teto diário partilhado nas chamadas à IA — `src/lib/ai/rateLimit.ts` ganhou um limite global de 800/dia, além do já existente 20/10min por utilizador). B1 ganhou 3 módulos novos (used-to, modais de dedução, Third Conditional) — 27→30 módulos de gramática seedados, B1 passa de 5 para 8 módulos (4+4), já não é o nível menos denso. Detalhe em `docs/decisions.md`. Antes, na mesma sessão: **LEITURA GRADUADA: ALVO DE 60 TEXTOS ATINGIDO** — `readingPassages.ts` 4→60 ao longo desta sessão (4 lotes: 20+10+10+7), distribuídos 10 Pre-A1 / 17 A1 / 17 A2 / 16 B1. Fecha completamente o último item de conteúdo em falta da auditoria original — o que resta agora é aprofundamento, não lacunas estruturais. Detalhe em `docs/decisions.md` ("Leitura graduada: alvo de 60 textos atingido"). Antes, na mesma sessão: **43/60 textos de leitura** — `readingPassages.ts` 33→43 num segundo lote de 10 (Pre-A1/A1 reforçados). Nota: o rascunho deste lote incluía um duplicado quase exato de um texto já existente ("The Weekly Market") — apanhado pela verificação de ids/títulos antes do commit e substituído, nunca chegou ao repositório. Ver `docs/decisions.md` 2026-08-27 ("Segundo lote"). Ainda faltam 17 para o alvo de 60 da auditoria. Antes, na mesma sessão: **mais 10 textos de leitura** — 23→33 (Pre-A1 reforçado de 1 para 3 textos, o mais fraco antes desse lote). Antes: **FASE 4: secção 294 da auditoria fechada (9/10 itens)**. 7 funcionalidades entregues nesta ronda (roleplay por cenário, ditado, métrica de confiança, rubrica de writing, conversa livre com objetivo, referência de Sons e Pronúncia, percurso visual de progressão "Palavra→Frase→Diálogo→Conversa" em `/speak`) cobrem 9 dos 10 itens da secção 294 (scoring de pronúncia e tempo de resposta já tinham sido feitos numa sessão anterior). **1 item genuinamente por fazer, com justificação em `docs/decisions.md`**: "guardar áudio para auto-avaliação" (incompatível com o pivot de custo zero — exigiria armazenamento pago a crescer sem limite por utilizador). Detalhe completo, item a item, em `docs/decisions.md` ("balanço da secção 294"). Antes desta ronda, na mesma sessão: **referência de Sons e Pronúncia** (`/practice/pronunciation`, 8 padrões de interferência PT→EN — TH, consoantes finais, acento tónico, vogais curtas/longas, R, letras mudas, clusters S-, -ED — com áudio via PlayTranscript). Antes: **conversa livre com objetivo** no Conversation Partner (small talk / fim de semana / opinião / notícia, `/speak`, mesma técnica de sessionFocus do setor/cenário — auditoria secção 294, item "conversa livre com objetivo"). Antes: **rubrica de writing** com 4 subscores (Gramática/Vocabulário/Coerência/Cumpre o pedido, 0-100 cada, mostrados como barras) além do feedback textual já existente, mesma técnica de linha-à-parte sanitizada já usada para SCORE/PRONUNCIATION. Antes, na mesma sessão: **roleplay por cenário** no AI Tutor (restaurante/hotel/aeroporto/reunião, `/speak`, personalidade `roleplay`), **Ditado** (`/practice/dictation`, 20 frases estáticas Pre-A1→B1, ouve-se e escreve-se o que se ouviu, sem custo via Web Speech API), e **métrica de confiança** em speaking (autoavaliação 1-5 depois do feedback, `SpeakingAttempt.confidenceSelfRating`, média visível em Progress). Detalhe completo em `docs/decisions.md` (topo do ficheiro). Antes disso: **FASE 3 FECHADA — pedido explícito do utilizador de continuar sem pausas e sem perguntas até os tokens da sessão se esgotarem. Conteúdo de referência estático expandido: `readingPassages.ts` 4→20 textos (~61→~2.400 palavras, cada um ligado a um conceito de gramática dos 27 módulos seedados), `idioms.ts` 8→26, `culturalTips.ts` 5→15, `sentencePatterns.ts` 8→18, `irregularVerbs.ts` 55→99 verbos (corrigido: `wake up` estava mal categorizado como irregular, é phrasal verb — substituído por `wake/woke/woken`). **Limpeza de schema morto**: removidos 5 modelos Prisma nunca escritos por código nenhum (`Question`, `UserVocabularyMastery`, `UserConceptMastery`, `Bootcamp`, `BootcampEnrollment`) e o enum `MasteryState`, confirmado por grep exaustivo sem referências na app. **PRONUNCIATION deixa de estar sempre a zero no octógono** — achado mais citado da auditoria original: `submitSpeaking()` agora pede à Gemini um score de pronúncia inferido do padrão do transcript (sinal indireto, não scoring fonético real, documentado como tal) e grava-o; também passou a gravar `responseTimeMs`. **"Inglês de hoje"** (secção 47 da auditoria): novo `src/lib/plan/dailyPlan.ts`, checklist concreta de atividades/minutos adaptada a `dailyMinutesTarget`, card clicável na Home (Standard e Intensive). Mais: `aria-live="polite"` em 6 runners de exercício que ainda não tinham, grelhas de atalhos responsivas até 4 colunas em ecrãs largos. Detalhe completo em `docs/decisions.md`. Antes disso: **META DE 2.000 PALAVRAS DE VOCABULÁRIO ATINGIDA** — pedido explícito do utilizador de continuar sem pausas até chegar lá. **Vocabulário total: 331 → 2.004 palavras** (era o maior gap identificado pela auditoria). 19 bancos de vocabulário standalone (`vocabulary-bank.json` a `vocabulary-bank-19.json`), cobrindo dezenas de categorias temáticas. Confirmado sem duplicados: 2.274 ids / 2.004 headwords em `content/curriculum/`, todos únicos — 13 colisões pontuais foram detetadas e corrigidas ao longo do processo pela verificação automática (grep de ids e headwords após cada lote, antes de qualquer commit). **Nota importante:** estas 2.004 palavras são vocabulário standalone (alimentam o Desafio Diário e a Revisão/SRS), não estão ligadas a lições do currículo — só os ~90 vocab items dos 22 módulos de gramática têm esse contexto pedagógico. Detalhe completo em `docs/decisions.md`. Antes disso: **Nível B1 introduzido** — `levels.json` ganhou B1 (Threshold), 2 sublevels (B1.1, B1.2), com 5 módulos novos: Past Perfect, Second Conditional, Passive Voice, Reported Speech, Future Continuous. **Cobertura de gramática agora 24/24** do checklist da auditoria — todos os conceitos identificados como "em falta" estão cobertos. Correção a uma nota anterior: introduzir B1 não exigiu alteração ao `prisma/schema.prisma` (o enum `CefrLevel` já incluía B1-C2 desde a Fase 0), foi só trabalho de conteúdo. 767 ids / 497 headwords em `content/curriculum/`, confirmados sem duplicados (desta vez verificados os dois, não só ids). Falta ainda: expansão de vocabulário até 2.000+ (~480 atualmente) e mais densidade de conteúdo B1 (só 5 módulos, menos denso que A1/A2). Detalhe em `docs/decisions.md`. Antes disso: **FASE 3 continuada** — mais 5 módulos de gramática A1/A2 (Future with will, Past Continuous, Gerunds vs. Infinitives, Question Tags, Relative Clauses) + `vocabulary-bank-3.json` com 122 palavras novas focadas em categorias antes ausentes (partes do corpo, saúde, emoções, trabalho, tecnologia). Cobertura de gramática ~18/24; os 5 conceitos que restam (past perfect, future continuous, segundo condicional, voz passiva, discurso indireto) exigem introduzir B1 no schema, reservado para depois. **Nota importante:** ao escrever este lote, introduzi 12 headwords duplicados contra o vocabulário já existente (verifiquei ids mas não headwords) — corrigido no mesmo lote, substituindo as entradas antigas dos bancos standalone. Confirmado: 702 ids / 482 headwords em `content/curriculum/`, sem duplicados em nenhum dos dois. Detalhe em `docs/decisions.md`. Total de vocabulário agora ~480 palavras (era 331 no início da auditoria). Antes disso: **FASE 3 da auditoria iniciada** (conteúdo) — 6 novos módulos de gramática A1 seedados (Present Continuous, Object/Possessive Pronouns, Wh- Questions, Articles, Quantifiers, Superlatives), cobertura de gramática 8/24→14/24 do checklist da auditoria. Cada JSON validado individualmente com PowerShell `ConvertFrom-Json` antes de entrar no `seed.ts` (sem Node.js local para correr o seed a sério) e confirmado sem colisões de id contra os 519 ids já existentes. Falta: os 7 conceitos gramaticais restantes (4 deles exigem introduzir B1 — Level/Sublevels novos, alteração maior), e a expansão de vocabulário (331→2.000+). Detalhe em `docs/decisions.md`. Antes disso: **FASE 2 da auditoria concluída** (fundações técnicas) — race conditions em streak/octógono corrigidas com `SELECT...FOR UPDATE` transacional, rate limiting no Gemini (20/10min por utilizador, `src/lib/ai/rateLimit.ts`), responsive real (`max-w-lg lg:max-w-2xl` nos 40 containers da app), `loading.tsx` único cobrindo todo o grupo `(app)`, "Continuar lição" já não salta lições abandonadas, `currentDay` do Intensive calculado a partir de `startDate` em vez de preso em 1, placement volta a poder recomendar A2, `qaApproved` finalmente filtrado, exportação RGPD completada, várias traduções PT em falta (octógono, certificado, "Day"→"Dia"). Detalhe completo em `docs/decisions.md`. Roadmap de fases em `docs/AUDITORIA-2026-08-26.md` secção 27 — Fase 3 (conteúdo: gramática/vocabulário/B1) é a próxima e a maior. Antes disso: **AUDITORIA MASTER completa** — relatório integral em **`docs/AUDITORIA-2026-08-26.md`** (nota global 4,2/10, matriz de melhorias, roadmap, plano de 4 meses, comparação com concorrentes). **23 correções aplicadas**, incluindo 2 críticas: (1) FK inválida que quebrava TODOS os exercícios dentro de lições, (2) correção feita no cliente que tornava notas e certificados forjáveis do browser. Ver `docs/decisions.md` para o detalhe técnico. **Achado central da auditoria: as fundações são boas, o conteúdo é que é insuficiente** — 11 lições ≈ 5 semanas de material para uma promessa de 4 meses. Antes disso: continuação do redesenho: correção de tradução mais rápida a sério (atalho de igualdade exata antes de chamar o Gemini, não só perceção), `TextField.tsx` novo aplicado a 13 inputs em 9 ficheiros (login/signup/passwords/onboarding/tutor), bug real corrigido (chat do tutor dizia sempre "The Coach is typing" independentemente da personalidade escolhida). Antes disso: **redesenho de interação estilo Busuu** (pedido explícito: botões maiores, campos de tradução melhores, mais intuitivo, "correção mais rápida", estilo profissional tipo Busuu) — `Button`/`Card`/`TextAreaField`/`Spinner` redesenhados (cantos mais arredondados, sombras, padding maior), `/practice` reorganizado em secções. **Isto substitui de propósito a escolha visual minimalista original da Fase 0** (`docs/09-sistema-design.md`) — ver `docs/decisions.md` para o registo completo. Filas de botões auditadas para `flex-wrap` (risco de transbordo com botões maiores).

Histórico completo e detalhado de todas as atualizações desta sessão (2026-08-26) está em `docs/decisions.md`, por ordem cronológica — este ficheiro só resume o estado atual, não repete cada decisão. Pontos-chave ainda válidos:
- Total de vocabulário ≈320 palavras (banco standalone + módulos) — utilizador pediu 20.000, decisão de honestidade documentada (não fabricar sem verificação).
- **Nenhum commit desde `b583b7a` foi validado por build real** (deploys Netlify pausados, ver aviso abaixo) — tudo revisto com cuidado extra, confirmar com um build assim que possível.

### ⚠️ Deploys da Netlify pausados até 2026-09-01 (créditos gratuitos esgotados)
O plano Free da Netlify tem 300 créditos/mês; esgotaram-se hoje (muitos deploys triggados nesta sessão longa). Commit `3753fb3` (e todos os seguintes) ficam "Canceled" em vez de publicar — **o código está no GitHub, só não constrói**. O site continua online normalmente na última versão publicada (`b583b7a`). Reinicia automaticamente a 1 de setembro de 2026 (ciclo de faturação corre 1-31 de cada mês) — nenhuma ação necessária, os pushes acumulados publicam-se sozinhos assim que os créditos renovarem. Alternativa (não usada, quebraria o requisito de custo zero): upgrade de plano, só com autorização explícita do utilizador. **Numa sessão nova antes de 2026-09-01**: não há forma de verificar deploys ao vivo — continuar a codificar/commitar/fazer push normalmente, mas rever o código com mais cuidado (sem o build da Netlify a apanhar erros de TypeScript em tempo real) e avisar já no início da sessão que os deploys estão pausados, sem repetir esta investigação.

### UX dos quizzes (correção de UX pedida pelo utilizador após testar)
Feedback imediato (Verificar → correto/incorreto + resposta certa → Seguinte) no Desafio Diário e Diagnóstico Semanal, TRANSLATION deixou de ficar de fora dos testes (vira pergunta de texto), Sheets de tema (`/practice/topic`) para escolher o que praticar, cor por pilar nos quizzes. Ver `docs/decisions.md`.

### Nota técnica — clicar em elementos da UI da Netlify via browser automation
A navegação por `find`/`ref` na consola da Netlify revelou-se instável (refs ficam obsoletos entre re-renders, timeouts de screenshot frequentes nesta página em concreto). **Solução mais fiável**: usar `javascript_tool` para localizar e clicar elementos diretamente no DOM (`document.querySelectorAll`, filtrar por `offsetParent !== null` para apanhar só o elemento visível/ativo, `.click()`), e para editar inputs React controlados usar o setter nativo do `HTMLInputElement.prototype` antes de disparar o evento `input` (`Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set.call(input, valor)`). Preferir este método a `find`/`computer` para esta consola específica.

## 1. O que é isto

App de aprendizagem de inglês para adultos falantes de português, CEFR Pre-A1→C2 (MVP1 cobre Pre-A1/A1). Ver `docs/00-visao-proposta-valor.md` para a visão completa e `docs/10-scope-mvp1.md` para o scope exato.

O utilizador (dono do projeto) **não instala nada localmente** — não tem Node.js, não sabe programar, Netlify, Supabase nem GitHub. Todo o ciclo de trabalho é: Claude edita código → `git push` → Netlify constrói e publica automaticamente → Claude verifica o log de build via browser e corrige o que falhar → repete até passar.

## 2. Estado atual (o que já está em produção)

- **Site em produção**: https://english-platafform.netlify.app (projeto Netlify renomeado pelo utilizador; o nome interno do site pode aparecer como `english-platafform` nalguns URLs da consola Netlify)
- **Repositório**: https://github.com/armandolacerda19-dotcom/EnglishClass (branch `main`, deploy contínuo automático a cada push)
- **Base de dados**: Supabase (`ingles-platafform`, projeto `cuvvsmqicvyewisshjoo`), Postgres via Prisma
- **IA**: Google Gemini (`gemini-2.0-flash`) — nível gratuito, ver `docs/decisions.md` (pivot de custo zero)
- **Speech-to-text e text-to-speech**: 100% no browser (Web Speech API), sem custo, sem serviço externo
- **Deploy**: `netlify.toml` corre `npx prisma generate && npx prisma db push --accept-data-loss && npx tsx prisma/seed.ts && npm run build` a cada push para `main`

### Fluxo funcional confirmado em produção (testado pelo utilizador)
- Landing page, signup, login
- Onboarding (objetivo, Standard/Intensive, tempo disponível, perfil)
- Placement test adaptativo (8 pilares)
- Currículo: **5 lições completas** seedadas (ver secção 4)
- AI Tutor ("The Coach") via Gemini
- Progress (octógono de competência, XP, streak, conquistas, checkpoints)
- Desafio Diário de vocabulário + Micro-Desafios (deploy `c67143e`/seguintes — ainda não confirmado pelo utilizador em produção, ver secção 6)
- Privacidade RGPD (exportar/eliminar dados)
- Instalação como app (PWA) — manifest + service worker mínimo, confirmado publicado e a funcionar (service worker registado em produção)
- Revisão espaçada real (SRS/SM-2) — `/practice/review`
- Octógono de competência vivo (atualiza com a prática, já não fica congelado no placement test)
- Diagnóstico Semanal (`/practice/weekly-test`), AI Tutor com 4 personalidades (`/speak`), collocations visíveis, 6 conquistas novas

### Histórico de deploy — lições aprendidas (não repetir)
Passámos por vários ciclos de build falhado antes do primeiro deploy bem-sucedido. Erros já corrigidos, não os reintroduzir:
1. `DIRECT_URL`/`DATABASE_URL` vazias → sempre confirmar que as env vars da Netlify têm mesmo valor (não só o nome criado) antes de assumir que um erro é de código.
2. Erro de tipo Prisma: `goal` do `IntensivePlan.update()` era `string`, não o enum `Goal` — usar cast explícito (`as any`) quando se passa um objeto solto a um `update`/`create` do Prisma cujos campos são enums.
3. `noUncheckedIndexedAccess` (tsconfig) torna `array[index]` em `T | undefined` — qualquer acesso posicional (`PLACEMENT_QUESTIONS[index]`, `lesson.steps[stepIndex]`) precisa de `!` ou de um guard explícito.
4. Callbacks `setAll(cookiesToSet)` do Supabase SSR precisam de tipo explícito (`{ name: string; value: string; options: CookieOptions }[]`) — TS estrito não infere `any` automaticamente.
5. Um valor colado manualmente numa env var da Netlify pode ficar corrompido (ex. caráter "•" residual do campo mascarado) — se aparecer um erro tipo "Cannot convert argument to a ByteString", suspeitar logo de env var corrompida, não de bug de código.

## 3. Stack e onde encontrar cada coisa

| Camada | Ferramenta | Ficheiro/pasta chave |
|---|---|---|
| Frontend/Backend | Next.js 14 App Router | `src/app/` |
| BD | Prisma + Supabase Postgres | `prisma/schema.prisma` |
| Auth | Supabase Auth (SSR) | `src/lib/supabase/`, `src/middleware.ts` |
| IA | Google Gemini | `src/lib/ai/gemini.ts` |
| Conteúdo curricular | JSON versionado | `content/curriculum/*.json`, seed em `prisma/seed.ts` |
| Design system | Tailwind + tokens | `tailwind.config.ts`, `docs/09-sistema-design.md` |
| Documentação Fase 0 | — | `docs/00-*.md` a `docs/11-*.md` |
| Decisões (log vivo) | — | `docs/decisions.md` — **atualizar sempre que uma decisão técnica mudar** |
| Deploy | Netlify | `netlify.toml` |

## 4. Conteúdo curricular seedado (11 lições — 8× A1 + 3× A2)

| Sublevel | Módulo | Conceito de gramática | Erro PT→EN destacado |
|---|---|---|---|
| Pre-A1 | First Words | Verbo "to be" | "I have 38 years" → "I am 38 years old" |
| A1.1 | Daily Life | Present Simple | Esquecer o -s na 3ª pessoa |
| A1.1 | About Me | Perguntas com Do/Does | "You like coffee?" → "Do you like coffee?" |
| A1.2 | Shopping | There is/There are | "há" invariável em PT vs. singular/plural em EN |
| A1.3 | Comparing Things | Comparativos | "more cheap" → "cheaper"; "more good" → "better" |
| A1.2 | At the Restaurant | Pedidos educados (Can/Could) | "I want a coffee" → "Can I have a coffee, please?" |
| A1.3 | Yesterday | Past Simple (regular/irregular) | "I go to the beach yesterday" → "I went to the beach yesterday" |
| A1.3 | Making Plans | Futuro com "going to" | "I going to travel" → "I am going to travel" |
| A2.1 | Life Experiences | Present Perfect (experiência) | "I visited Paris" (sem data) → "I have visited Paris" |
| A2.1 | Rules and Obligations | Must/Have To/Should | "You must drink more water" (conselho leve) → "You should drink more water" |
| A2.2 | Real Possibilities | First Conditional (if + Present Simple, will) | "If it will rain" → "If it rains" |

**Nota sobre A2**: só 3 módulos — é o início do nível, não um currículo A2 completo (isso é o item #6 da lista de melhorias, sinalizado como "fundação, não conclusão" em `docs/decisions.md`). `levels.json` já tem A2.1/A2.2/A2.3 definidos, prontos para mais módulos numa sessão futura.

~24 itens de vocabulário nestes módulos + **banco standalone em 2 vagas** (`vocabulary-bank.json` ~135 + `vocabulary-bank-2.json` ~162, `seedVocabularyBank()`/`VOCABULARY_BANKS` em `prisma/seed.ts`) = **≈320 palavras no total**. Ver `content/curriculum/` para o JSON completo de cada um. Cada módulo novo alimenta automaticamente o Diagnóstico Semanal e as Sheets de tema (`/practice/topic`), que reutilizam os `Exercise` já seedados — mais conteúdo = mais variedade nesses dois sítios sem precisar de código novo. O banco standalone alimenta diretamente o Desafio Diário e a Revisão (SRS), sem precisar de lição/módulo associado. **O utilizador pediu explicitamente um mínimo de 20.000 palavras** — ver `docs/decisions.md` (duas entradas, 2026-08-26) para a decisão de honestidade repetida: não fabricar milhares de entradas sem verificação, preferir vagas sucessivas e reais. `VOCABULARY_BANKS` já está preparado para receber mais vagas (`vocabulary-bank-3.json`...) em sessões futuras sem alterar a lógica de seed.

Também: **verbos irregulares** (`src/content/irregularVerbs.ts`, `/practice/verbs`, 99 verbos) e **construção frásica** (`src/content/sentencePatterns.ts`, `/practice/patterns`, 18 padrões de ordem de palavras) — conteúdo estático, mesmo padrão de `readingPassages.ts`/`culturalTips.ts`, sem precisar de schema/seed. `readingPassages.ts` tem 60 textos (Pre-A1 a B1, alvo de 60 da auditoria atingido), `idioms.ts` 26 expressões, `culturalTips.ts` 15 dicas, `pronunciationTips.ts` 8 padrões de pronúncia, `dictation.ts` 20 frases — ver `docs/decisions.md` 2026-08-27 para o detalhe das expansões.

## 5. Variáveis de ambiente na Netlify (nomes, não os valores — ver gestor de password/Netlify para os valores reais)

`DATABASE_URL`, `DIRECT_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SITE_URL` (deve ser `https://english-platafform.netlify.app`), `GEMINI_API_KEY`.

**Regra de segurança que o Claude segue sempre**: nunca escrever passwords/API keys/tokens em campos de formulários (Netlify, Supabase, etc.), mesmo com autorização explícita do utilizador. Só o utilizador cola esses valores. Claude pode e deve fazer todo o resto por browser automation (criar variáveis vazias com o nome certo, navegar, disparar deploys, ler logs).

## 6. Próximos passos pedidos pelo utilizador (2026-08-26)

- [x] Documento do projeto/fases/atualizações — entregue como `.rtf` (não `.docx`: sem Node/Python/pandoc nesta máquina, ver `docs/decisions.md`), enviado ao utilizador via `SendUserFile`. **Não está no repositório** — se for pedido de novo, o script fonte ficou em `PROJECT_STATE.md` (esta secção) como referência do conteúdo; recriar a partir das secções deste ficheiro + `docs/`.
- [x] Desafio Diário de vocabulário (5-10 palavras, escolha múltipla, + frases de exemplo) — `src/lib/dailyChallenge.ts`, rota `/practice/daily-challenge`.
- [x] Checkpoints diários/semanais/mensais — `src/lib/checkpoints.ts`, cartão em `/progress`.
- [x] Micro-Desafios ("5 Minutos Matinais", "Casa de Banho", "Sofá") — implementados diretamente (utilizador pediu para avançar sem esperar confirmação): `src/lib/microChallenges.ts`, rota `/practice/micro-challenges`.
- [x] 6ª lição adicional (A1.2 "At the Restaurant" — pedidos educados Can/Could) — para reforçar a profundidade do currículo enquanto havia orçamento de tokens.
- [x] `NEXT_PUBLIC_SITE_URL` corrigido para `https://english-platafform.netlify.app` e redeploy confirmado publicado.
- [ ] **Por confirmar pelo utilizador em produção**: clicar em Desafio Diário e Micro-Desafios no site real (deploy `ea0214e` já publicado, deploy código-fonte confirmado sem erros; falta só confirmação visual/funcional do próprio utilizador).
- [x] App instalável no Android (PWA) — `public/manifest.webmanifest`, `public/icon.svg`, `public/icon-maskable.svg`, `public/sw.js`, `src/components/PwaRegister.tsx`, `src/app/layout.tsx`, `src/middleware.ts` (exclusão do matcher). Ver `docs/decisions.md` 2026-08-26. Deploy confirmado publicado, manifest/ícone/service worker verificados em produção. **Nota**: ícones em SVG, não PNG — considerar gerar PNGs (192×192, 512×512) numa sessão futura com Node/Python disponível. Falta só o utilizador confirmar no telemóvel: Chrome Android → menu (⋮) → "Adicionar ao ecrã principal".
- [x] **SRS real (SM-2)** — crítica dura ao produto pedida pelo utilizador (2026-08-26), priorizado por ele como #1: `src/lib/srs/sm2.ts` (algoritmo), `src/lib/srs/schedule.ts` (agendamento/consulta), `/practice/review` (fila estilo Anki). Ligado ao Desafio Diário (cada palavra agenda revisão) e aos erros de exercícios de lição (dedupe por `errorType`, agenda revisão do erro). Badge de pendentes em Home e Prática. Deploy `49164d0` confirmado publicado.
- [x] **Octógono de competência deixa de ficar congelado** — `src/lib/skillProfile.ts` (`updateSkillScore`, EMA α=0.25), ligado a exercícios/writing/speaking/translation/Desafio Diário/revisões SM-2. Também preenche `WritingAttempt.score`/`SpeakingAttempt.fluencyScore` (existiam no schema, nunca usados) via parsing de `SCORE: NN` na resposta do Gemini. Ver `docs/decisions.md`.
- [ ] **Lista de melhorias priorizada com o utilizador (crítica de 2026-08-26), por ordem de prioridade acordada**:
  1. ~~SRS (repetição espaçada real)~~ — feito.
  2. ~~Octógono de competência vivo~~ — feito (descoberto durante a continuação "updates com mais impacto"). Deploy `2982d6a` confirmado publicado.
  3. ~~Testes/exames periódicos~~ — feito: Diagnóstico Semanal (`/practice/weekly-test`, `src/lib/weeklyTest.ts`).
  4. ~~Inglês profissional genérico cedo~~ — feito: `interviewer`/`conversation_partner`/`native_friend` desbloqueados em `/speak` (`src/lib/ai/personalities.ts`).
  5. Expansão de vocabulário e currículo (A2+), phrasal verbs/idiomas novos (distintos dos `related_forms` já existentes, agora mostrados), listening mais natural, leitura extensiva — itens de custo mais alto, ver crítica completa na conversa de 2026-08-26 para a lista completa com custo/impacto.
- [x] **5 updates adicionais (pedido "faça no mínimo 5 updates")**: Diagnóstico Semanal, AI Tutor multi-personalidade, collocations visíveis, 6 novas conquistas, entry points em Home/Practice. Ver `docs/decisions.md` 2026-08-26 para detalhe de cada um. Deploy `b5f6400` confirmado publicado.
- [x] **Correção de UX pós-teste do utilizador**: feedback imediato por pergunta (não silencioso), TRANSLATION deixou de faltar nos testes (vira texto livre em vez de MC), Sheets de tema (`/practice/topic/[pillar]`, `src/lib/practiceQuestions.ts`), cor por pilar (`src/lib/pillarDisplay.ts`) nos quizzes. Ver `docs/decisions.md` para o porquê de o redesenho visual mais amplo ("cores pesadas" no fundo/hero) ter ficado por fazer nesta ronda — precisa de iteração com screenshots, não de uma mudança às cegas.
- [x] **Lista de 19 melhorias — fechada** (pedido explícito: "termina todas as atualizações das 19 que ainda não estão feitas... quero todas de uma só vez"). Estado final de cada item, ver tabela completa na conversa e detalhe em `docs/decisions.md`:
  - Feitos nesta ronda final: #3 (idiomas dedicado), #8 (voz mais natural), #9 (cultura), #12 (Modo Imersão), #13 (PWA offline parcial), #14 (entrevista por setor), #16 (analytics básico), #17 (certificação sem QR real), #18 (leitura facilitada, parcial).
  - Já feitos antes: #1 (SRS), #2/#5 (conteúdo — parcial, 9 lições), #4 (roleplay), #10 (multi-personalidade), #15 (Diagnóstico Semanal).
  - **Genuinamente não feito, com justificação em `docs/decisions.md`**: #6 (A2 "completo" — só a fundação foi feita), #19 (comunidade — incompatível com o modelo 1:1 atual, precisaria de infraestrutura nova).
  - #7 (writing "completo") já estava razoavelmente bom (score numérico); não recebeu trabalho extra nesta ronda por ser melhoria incremental, não gap em falta.

Ver o corpo da conversa da sessão de 2026-08-26 para o pedido exato.

## 7. Convenções a manter

- Interface em português europeu.
- Sem comentários supérfluos no código — só onde a razão não é óbvia.
- Toda a decisão de arquitetura nova → registar em `docs/decisions.md` com data.
- Toda a mudança relevante na app → atualizar este ficheiro (`PROJECT_STATE.md`).
- Nunca expandir scope sem avisar o utilizador primeiro (regra do master prompt original).
- Antes de um deploy, confirmar mentalmente riscos de `noUncheckedIndexedAccess` e tipos de enum do Prisma (ver secção 2, "lições aprendidas").
- **Direção visual (2026-08-26)**: o produto passou de um estilo minimalista/discreto (cantos de 6px, sem sombras) para um estilo mais "app profissional tipo Busuu" (cantos de 14px/18px, sombras suaves, botões grandes) — pedido explícito do utilizador, substitui a escolha original de `docs/09-sistema-design.md`. Ver `docs/decisions.md` 2026-08-26 ("Redesenho de interação"). Não voltar ao estilo antigo sem confirmar com o utilizador.
